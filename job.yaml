apiVersion: batch/v1
kind: Job
metadata:
  name: mybatch
spec:
  completions: 5
  parallelism: 2
  template:
    metadata:
      labels:
        app: myjob
    spec:
      restartPolicy: OnFailure
      containers:
        - name: main
          image: luksa/batch-job
